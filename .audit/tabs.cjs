const {chromium}=require('playwright');const fs=require('fs');
(async()=>{const browser=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});await page.goto('https://dev-nsnn.thehegeo.com/?year=2026&acc=PERIOD&item=tong-so&tab=overview');await page.waitForTimeout(2000);for(const [name,tab] of [['detail','Chi tiết địa bàn'],['compare','So sánh hai kỳ']]){await page.getByRole('tab',{name:tab,exact:true}).click();await page.waitForTimeout(700);const text=await page.locator('body').innerText();fs.writeFileSync('.audit/'+name+'.txt',text);console.log(name,page.url(),text.slice(text.indexOf('Reset zoom')));console.log('SELECTS',await page.locator('select').evaluateAll(es=>es.map(e=>({id:e.id,value:e.value,options:[...e.options].map(o=>({text:o.text,value:o.value}))}))));await page.screenshot({path:'.audit/'+name+'.png',fullPage:true});}await browser.close()})();
