---
target: toàn bộ dự án
total_score: 22
max_score: 40
na_heuristics: 
p0_count: 3
p1_count: 2
target_identity: "file:C:\\Users\\admin\\Desktop\\Prototype overview NSNN\\web\\src"
timestamp: 2026-09-14T07-17-43Z
slug: src
---
Method: dual-agent (A: rà soát thiết kế · B: bằng chứng đo được)

## Điểm sức khoẻ thiết kế — 22/40 · Chấp nhận được, cần cải thiện đáng kể

| # | Heuristic | Điểm | Vấn đề then chốt |
|---|---|---|---|
| 1 | Hiển thị trạng thái hệ thống | 3 | Nhãn "Dữ liệu mô phỏng" bị display:none dưới 768px |
| 2 | Tương ứng thế giới thực | 2 | Không có ngày chốt số liệu; "Tiến độ so dự toán" không mang chữ mô phỏng |
| 3 | Kiểm soát và tự do | 2 | Nút "Đặt lại bộ lọc" chỉ render khi open===true → desktop rộng không có |
| 4 | Nhất quán và tiêu chuẩn | 2 | Bốn cách nói về cùng tập 126 phường/xã trong một màn hình |
| 5 | Phòng ngừa lỗi | 2 | Tab So sánh "Theo nguồn thu" âm thầm trả toàn số 0 |
| 6 | Nhận ra thay vì phải nhớ | 1 | Panel chi tiết địa bàn không hiện tên phường/xã; tâm donut luôn in "100%" |
| 7 | Linh hoạt và hiệu quả | 3 | Thiếu hẳn xuất/sao chép |
| 8 | Thẩm mỹ và tối giản | 3 | Hai nút dev nặng đô hơn chip cảnh báo số liệu giả |
| 9 | Nhận biết & khắc phục lỗi | 3 | role="alert", nút thử lại, cảnh báo lệch cầu nối |
| 10 | Trợ giúp và tài liệu | 1 | Mọi giải thích quan trọng chỉ nằm trong title |

## Phán quyết tính đặc thù
Dashboard analytics SaaS kỷ luật, đội mũ navy. Đổi "Thu ngân sách"→"Doanh thu", "phường/xã"→"cửa hàng" thì không quyết định bố cục nào cần sửa. Giao diện không mượn gì từ biểu mẫu báo cáo thu NSNN thật — hàng năm cột Dự toán · Thực hiện · Lũy kế · %dự toán · %cùng kỳ. Dashboard có đủ năm con số nhưng rải ra bốn widget.

Quét tự động: detector 0 findings / 31 file, NHƯNG chạy DEGRADED (thiếu htmlparser2, css-select, css-tree, domutils) → con số dưới thực tế. Không có overlay (cần puppeteer đầy đủ).

## Nền móng kỹ thuật đã vững (đo trên 72 tổ hợp width×route)
0 tràn ngang · 0 thẻ cắt nội dung · 0 lỗi JS · 0 chuỗi rác · 0 lỗi tương phản chữ trên 73 cặp màu · 0 vùng chạm dưới 24px · heading sạch · prefers-reduced-motion xử lý đúng cả d3 lẫn CSS · zoom 200% sạch.

## Điểm mạnh
1. Kỷ luật đơn vị tiền — một thang cho cả tập, đơn vị một lần ở đầu cột, tabular numerals, giữ nguyên qua bốn tab.
2. Thành thật về chỗ mình yếu — ô "Cần chú ý" nêu đích danh; "Chưa có kỳ trước" thay vì phần trăm vô nghĩa; đường xu hướng đứt quãng; waterfall tự cảnh báo khi lệch.
3. Phòng lỗi ở tầng dữ liệu — danh sách kỳ chỉ có kỳ thực có dữ liệu; nguồn khác phạm vi bị disabled kèm chữ giải thích trong <option>.

## Vấn đề ưu tiên

### P0 — Tab So sánh tự mâu thuẫn
build.ts:536-537 gán sideA = base; sideB = base ở mode revenue → mỗi dòng bảng có a===b, delta 0, cầu nối +0, trong khi KPI (scopeA/scopeB khác nhau) nói −50.414 tỷ. Mode period: rowItems chỉ lấy DOMESTIC_ITEMS nên cầu nối 43.754→50.718 đeo nhãn kỳ y hệt KPI 48.616→56.117. Hai con số trái ngược cách nhau 20px.
Lệnh: /impeccable harden

### P0 — Panel chi tiết không nói đang nói về địa bàn nào
data.location.name chỉ tồn tại trong aria-label (LocationDetailTab.tsx:152). Người dùng nhìn thấy ít hơn người dùng screen reader. Thêm h2 tên phường; bỏ ô KPI "Mã địa bàn" thay bằng "Lũy kế từ đầu năm".
Lệnh: /impeccable clarify

### P0 — Nhãn "số liệu mô phỏng" biến mất dưới 768px
dashboard.css:1111 ẩn .dheader-source → khổ iframe 500px và mobile không còn chữ nào nói số liệu là giả. Nhãn KPI trung tính 12px, "66,7%" thì 23px/700.
Lệnh: /impeccable adapt

### P1 — Số bị cắt ở bảng, và bảng không cuộn được
Ở 360/390/500/768px div.dtable-wrap có overflow-x:auto nhưng scrollWidth===clientWidth → không cuộn, "+14,8% so cùng kỳ" cắt còn 62px là mất hẳn. span.dcontrib-name (270/156) và .dbar-label b (228/171) không có title.
Lệnh: /impeccable adapt

### P1 — Bốn mẫu số cho cùng tập 126 phường/xã
Thay bằng một dòng phạm vi duy nhất dưới thanh lọc: "Toàn thành phố · 124/126 phường, xã có số liệu · Đơn vị tính: tỷ đồng · Số liệu chốt đến …" — cũng là dòng đầu biểu mẫu giấy.
Lệnh: /impeccable clarify

### P2 — Thanh mã hoá một đại lượng, số bên cạnh in đại lượng khác
Thẻ "Tăng trưởng địa bàn": thanh dài nhất nằm cạnh số nhỏ nhất. Số chính phải in +63,7%.
Lệnh: /impeccable layout

### P2 — Donut: tâm vô nghĩa + ba lỗi tương phản phi-văn-bản
Tâm luôn in "100%", không phản hồi lựa chọn. circle.ddonut-segment.is-interactive 1,91:1 (cần 3:1); path.dchart-line.is-previous 2,83:1. Focus trên cung donut chỉ đổi stroke-width 22→24.
Lệnh: /impeccable colorize

## Cờ đỏ theo persona
Alex: không có xuất/sao chép; rankDirection/growthDirection/showTrendTable/selectedBudgetLevel là useState không vào URL; danh sách 126 dòng cuộn không có chỉ báo.
Sam: title là vật mang duy nhất của mọi giải thích; chỉ Tổng quan có "Xem bảng số liệu"; 5 điểm dừng Tab trên cung donut không có chỉ dấu focus. Điểm cộng: Caret SVG kèm mọi chỉ báo, vùng chạm theo pointer:coarse.
Chị Hằng (trưởng phòng đọc số trong họp): khổ 500px không có chữ nào nói số là mô phỏng; tab So sánh cho hai đáp án; không có chỗ trả lời "số chốt đến ngày nào".

## Trục kiến trúc và tài liệu
- Ranh giới tầng sạch: không import chéo feature, domain không import ngược, 0 TODO, 6 phụ thuộc runtime. Còn 2 file gọi thẳng data/mock/.
- Hợp đồng dữ liệu theo hình dạng WIDGET không theo nghiệp vụ: OverviewData 13 trường, RevenueAnalysis 9, LocationDetail 8, AdvancedComparison 10 — mỗi trường một thẻ. Backend phải biết bố cục dashboard.
- Tài liệu lệch code: THIET-KE §7 vẫn ghi KPI 3 là "Độ phủ địa bàn"; BA-NSNN.md không nhắc chữ "dự toán" lần nào dù đó là số bịa nhiều nhất; §15 dừng ở A08, nên có A09.
- Ba con số tiêu chí nghiệm thu không khớp (BA 16 · THIET-KE 18 · script 17); phan-tich.md trùng byte ở hai nơi.

## Quan sát nhỏ
Hai nút dev nổi hơn chip cảnh báo · Bốn cách viết cho "thiếu dữ liệu" · Trạng thái rỗng quên nhắc bản đồ · Select "Chỉ tiêu" 660px cho 3 lựa chọn còn "Năm" 70px cho 10+ · "NS cấp huyện −56 tỷ" không giải thích · Thẻ location-detail lệch 44–55px ở 1024–1920px · 16 ERR_ABORTED geojson là artifact React StrictMode ở dev, không phải lỗi.

## Câu hỏi đáng nghĩ
1. Nếu xoá dải navy và đổi "phường/xã" thành "cửa hàng", có ai nhận ra đây là dashboard ngân sách nhà nước không?
2. Con số nào chị Hằng dám đọc lên trong họp — giao diện đã làm gì để chị biết con số nào thì không?
3. Vì sao hệ thống đã có sẵn cơ chế !data.reconciled lại chưa áp cho quan hệ giữa dải KPI và cầu nối bên dưới?
